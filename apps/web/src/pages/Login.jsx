import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../lib/api';
export default function Login(){const[u,setU]=useState('admin'),[p,setP]=useState(''),[e,setE]=useState('');const nav=useNavigate();return <div className="login"><form onSubmit={async ev=>{ev.preventDefault();try{await api.login(u,p);nav('/admin')}catch(x){setE(x.message)}}}><h1>ZONOE 后台</h1><input value={u} onChange={x=>setU(x.target.value)} placeholder="用户名"/><input type="password" value={p} onChange={x=>setP(x.target.value)} placeholder="密码"/><button>登录</button>{e&&<p className="error">{e}</p>}</form></div>}
