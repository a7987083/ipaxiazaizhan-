import {useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import {api} from '../lib/api'; import AppCard from '../components/AppCard';
export default function Home(){const[d,setD]=useState(null),[err,setErr]=useState('');useEffect(()=>{api.home().then(x=>setD(x.data)).catch(e=>setErr(e.message))},[]);if(err)return <div className="wrap state">{err}</div>;if(!d)return <div className="wrap state">加载中…</div>;return <div className="wrap home"><section className="hero"><div><small>ZONOE · iOS 应用下载</small><h1>{d.settings.hero_title||'探索更多可能，让优秀的应用触手可及'}</h1><p>统一聚合多个现有 MySQL 软件源，安装包继续走原来的 IPA / NAS / CDN 地址，不重复占用存储。</p><Link className="hero-btn" to="/apps">发现精彩应用</Link></div></section><div className="notice">📢 <b>站点公告</b><span>{d.settings.site_notice||'欢迎使用 ZONOE 下载站'}</span></div><section><div className="section-head"><h2>🔥 热门应用</h2><Link to="/apps?sort=downloads">查看全部</Link></div><div className="grid">{d.hot.map(x=><AppCard key={x.id} app={x}/>)}</div></section><section><div className="section-head"><h2>🆕 最新更新</h2><Link to="/apps">查看全部</Link></div><div className="grid">{d.latest.map(x=><AppCard key={x.id} app={x}/>)}</div></section><section><div className="section-head"><h2>软件源</h2></div><div className="categories">{d.categories.map(c=><Link key={c.id} to={`/apps?category=${c.slug}`}>{c.name}</Link>)}</div></section></div>}
